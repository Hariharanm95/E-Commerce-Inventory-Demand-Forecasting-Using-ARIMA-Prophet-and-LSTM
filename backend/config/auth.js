// Configuration of JWT secret will be done in the .env file, this file is not needed
// module.exports = {
//     jwtSecret: process.env.JWT_SECRET || 'your-default-jwt-secret', // Fallback for dev
// };